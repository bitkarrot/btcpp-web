<!DOCTYPE html>
<html>
  <head>
    <title>bitcoin++ | Check out!</title>
    <link rel="stylesheet" href="/static/css/mini.css" />
    <link rel="stylesheet" href="/static/css/payment.css" />
    <script src="/static/js/script.js" type="text/javascript"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>✨</text></svg>">
  </head>
  <body  onload="onLoaded()">
  {{ block "mainnav" . }} {{ end }}
    <section id="success">
       <div class="relative overflow-hidden pt-8 sm:pt-16">
          <div class="mx-auto max-w-7xl px-6 pt-4 pb-12 lg:px-8">
            <div class="relative bg-gray-900 rounded-2xl">
              <div class="relative h-80 overflow-hidden md:absolute md:left-0 md:h-full md:w-1/3 lg:w-1/2">

                <img class="h-full w-full object-cover rounded-t-2xl md:rounded-l-2xl md:rounded-tr-none" src="/static/img/{{ .Conf.Tag }}.png" alt="">
              </div>
            <div class="relative mx-auto max-w-7xl py-24 sm:py-32 lg:px-8 lg:py-40">
              <div class="pl-6 pr-6 md:ml-auto md:w-2/3 md:pl-16 lg:w-1/2 lg:pl-24 lg:pr-0 xl:pl-32">
                <p class="text-3xl font-bold tracking-tight text-white sm:text-4xl">Let's get you registered</p>
                <h2 class="mt-2 text-gray-300 font-semibold leading-7">{{ .Conf.Desc }}</h2>
                <p class="mt-6 text-base leading-7 text-gray-300">Final boss is making a bitcoin payment</p>

                <div class="mt-12 grid grid-cols-1 gap-x-8 text-base w-full sm:w-2xl">
                  <div class="payment-toggle">
                    <button class="toggle-btn active" onclick="togglePayment('lightning')">Lightning</button>
                    <button class="toggle-btn" onclick="togglePayment('onchain')">On Chain</button>
                  </div>
                  <div id="lightning-option" class="payment-option active">
                    <div class="qr-container">
                      <img src="data:image/png;base64,{{ .BoltQR }}" alt="Lightning QR Code" />
                    </div>
                    <p class="mt-6 text-base leading-7 text-gray-300">Invoice for {{ .SatsPrice }}sats (${{ .Price }}USD)</p>
                    <div class="copy-container">
                      <div class="copy-text">{{ .Invstring }}</div>
                      <button class="copy-btn" onclick="copyToClipboard(this, '{{ .Invstring }}')">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
                          <path d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/>
                        </svg>
                      </button>
                    </div>
                  </div>
                  {{ if ne .BIP21QR "" }}
                  <div id="onchain-option" class="payment-option">
                    <div class="qr-container">
                      <img src="data:image/png;base64,{{ .BIP21QR }}" alt="On Chain QR Code" />
                    </div>
                    <p class="mt-6 text-base leading-7 text-gray-300">Pay {{ .BtcPrice }} (${{ .Price }}USD)</p>
                    <div class="copy-container">
                      <div class="copy-text">{{ .BtcAddr }}</div>
                      <button class="copy-btn" onclick="copyToClipboard(this, '{{ .BtcAddr }}')">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
                          <path d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/>
                        </svg>
                      </button>
                    </div>
                  </div>
                  {{ end }}
                </div>

              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
    <script>
    const onLoaded = () => {
      let eventSource = new EventSource("/tix/{{ .InvLabel }}/check-status");
      eventSource.onmessage = (event) => {
        if (event.data === "true") {
          window.location.href = "/conf/{{ .Conf.Tag }}/success"
        } else {
          window.location.href = "/conf/{{ .Conf.Tag }}"
        }
      };
    };

    function togglePayment(option) {
      document.querySelectorAll('.toggle-btn').forEach(btn => btn.classList.remove('active'));
      document.querySelector(`button[onclick="togglePayment('${option}')"]`).classList.add('active');

      document.querySelectorAll('.payment-option').forEach(div => div.classList.remove('active'));
      document.getElementById(`${option}-option`).classList.add('active');
    }

    function copyToClipboard(button, text) {
      navigator.clipboard.writeText(text).then(() => {
        const icon = button.querySelector('svg');
        const originalPath = icon.innerHTML;
        icon.innerHTML = '<path d="M9 16.2L4.8 12l-1.4 1.4L9 19 21 7l-1.4-1.4L9 16.2z"/>'; // checkmark icon
        button.classList.add('copied');
        setTimeout(() => {
          icon.innerHTML = originalPath;
          button.classList.remove('copied');
        }, 2000);
      }).catch(err => {
        console.error('Failed to copy text: ', err);
      });
    }
    </script>
  </body>
</html>
